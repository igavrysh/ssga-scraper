package com.orbis.ssgascraper.webscraper;

/**
 * Interface to create the required methods, as iteration is done over the enum
 * and expecting that the method is present.
 */
public interface Scraper {
    void startScraper();
}
